import { PricingFeature } from "@/features/pricing";

export const metadata = {
  title: "قیمت‌گذاری | پنل شدکس",
  description: "پلن‌ها و قیمت‌های اشتراک",
};

export default function PricingPage() {
  return <PricingFeature />;
}
