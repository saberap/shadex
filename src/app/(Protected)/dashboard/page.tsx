import { DashboardFeature } from "@/features/dashboard";

export const metadata = {
  title: "داشبورد | پنل شدکس",
  description: "نمای کلی داشبورد مدیریت",
};

export default function DashboardPage() {
  return <DashboardFeature />;
}
